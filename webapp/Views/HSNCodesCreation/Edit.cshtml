@{
    ViewBag.Title = "Edit";
    //Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = null;
}
@model SmartAdminMvc.Models.TBL_HSN_MASTER



    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">

        <div class="jarviswidget jarviswidget-color-teal" id="wid-id-1" data-widget-editbutton="false" data-widget-custombutton="false" data-widget-deletebutton="false">

            <header>
                <span class="widget-icon"> <i class="fa fa-edit"></i> </span>
                <h2 style="font-weight:bold;">HSN Edit</h2>
            </header>

            <!-- widget edit box -->
            <div class="jarviswidget-editbox">
                <!-- This area used as dropdown edit box -->

            </div>
            <form action="@Url.Action("Edit","HSNCodesCreation")" method="post" id="hsn-edit" novalidate="novalidate" class="smart-form">
                @*@using (Html.BeginForm("Edit", "HSNCodesCreation", FormMethod.Post, new { @id = "hsn-edit" }))
                    {*@
                @*@Html.AntiForgeryToken()*@

                <div id="comment-form">
                    <fieldset>
                        @Html.ValidationBootstrap()
                        @*@Html.ValidationSummary(true, "", new { @class = "text-danger" })*@
                        @Html.HiddenFor(model => model.hsnId)
                        <div class="row">

                            <section class="col col-6">
                                <div class="form-group">
                                    <label>HSN Code</label>
                                    <label class="input">

                                        @Html.TextBoxFor(model => model.hsnCode, new { @id = "hsncode", @maxlength = "8", @required = true })
                                        @Html.ValidationMessageFor(model => model.hsnCode, "", new { @class = "text-danger" })
                                    </label>

                                </div>
                            </section>
                          
                            <section class="col col-6">
                                <div class="form-group">
                                    <label>Item Description</label>
                                    <label class="input">

                                        @Html.TextBoxFor(model => model.hsnDescription, new { @id = "hsndescription", @required = true })
                                        @Html.ValidationMessageFor(model => model.hsnDescription, "", new { @class = "text-danger" })
                                    </label>

                                </div>
                            </section>
                        </div>
                        <div class="row">
                            @*<section class="col col-6">
                                <div class="form-group">
                                    <label>Unit Price</label>
                                    <label class="input">

                                        @Html.TextBoxFor(model => model.unitPrice, new { @id = "unitprice", @required = true })
                                        @Html.ValidationMessageFor(model => model.unitPrice, "", new { @class = "text-danger" })
                                    </label>

                                </div>
                            </section>*@
                          
        
                            <section class="col col-6">
                                <div class="form-group">
                                    <label>Rate</label>
                                    <label class="input">

                                        @*@Html.EditorFor(model => model.rate, new { @style = "disable:true", @id = "rate" })*@
                                        @Html.DropDownListFor(model => model.rate, new SelectList(ViewBag.Rates, "ratevalue", "ratevalue", ViewBag.Rate), "--Select--", new { @Class = "form-control input-sm", @id = "rate", @required = true })
                                        @Html.ValidationMessageFor(model => model.rate, "", new { @class = "text-danger" })
                                    </label>

                                </div>
                            </section>
                        </div>
                    </fieldset>
                    <div class="col text-center">

                        <input type="submit" value="Update" id="btnSubmit" class="btn btn-lg btn-active" />


                    </div>
                </div>
                @*}*@
            </form>
        </div>
    </article>

    <div>
        @Html.ActionLink("Back", "Index")
    </div>

    @section pagespecific
            {
        <script type="text/javascript">

            $('#hsncode').bind('keypress', numericInput);
            $(function () {
                // Validation
                $("#hsn-edit").validate({
                    // Rules for form validation
                    rules: {
                        hsnCode: {
                            required: true
                        },
                        hsnDescription: {
                            required: true
                        },
                        unitPrice: {
                            required: true,
                            number: true
                        },
                        rate: {
                            required: true,
                            number: true
                        }
                    },

                    // Messages for form validation
                    messages: {
                        hsnCode: {
                            required: 'Please enter HSN Code'
                        },
                        hsnDescription: {
                            required: 'Please enter Item Description'
                        },
                        unitPrice: {
                            required: 'Please enter Unit Price',
                            number: 'Please enter numbers only'
                        },
                        rate: {
                            required: 'Please select Rate',
                            number: 'Please enter numbers only'
                        }

                    },

                    // Do not change code below
                    errorPlacement: function (error, element) {
                        error.insertAfter(element.parent());
                    }
                });
            });

        </script>

    }

    @*<script type="text/javascript">
            function check(){
            var hsncode = document.getElementById("hsncode").value;
            var hsndescription = document.getElementById("hsndescription").value;
            var unitprice = document.getElementById("unitprice").value;
            var rate = document.getElementById("rate").value;
            if(hsncode=="" || hsndescription=="" || unitprice=="" )
            {
                return false;
            }

            }
        </script>*@

